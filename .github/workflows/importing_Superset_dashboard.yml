name: importing Superset dashboard

on: push
jobs:
  my-job:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
        
      - name: API-step
        run: 
           bash ./rest_API.sh

