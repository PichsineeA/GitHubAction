name: print-hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: print-step
        run: 
          git update-index --chmod=+x ./os_check.sh
          ./os_check.sh ${{ runner.os }}
        shell: bash
